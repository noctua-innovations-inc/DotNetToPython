@page "/"

<div class="container">
    <h3>Llama 3.2 3B Instruct Demo</h3>

    <textarea @bind="Prompt" placeholder="Enter your prompt here" class="form-control"></textarea>

    <button @onclick="GenerateText" disabled="@IsProcessing" class="btn btn-primary mt-3">
        @if (IsProcessing)
        {
            <span>Processing...</span>
        }
        else
        {
            <span>Generate</span>
        }
    </button>

    <p class="mt-3">@GeneratedText</p>
</div>
